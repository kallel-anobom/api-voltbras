"""chamar o type de
SuitablePlanet, não é a table, estamos falando do Type
por isso ficava estranho a função getSuitablePlanetses
renomeando, podemos usar getSuitablePlanets"""
type SuitablePlanet {
  id: ID! @id 
  name: String!
  """massa é um float"""
  mass: Float!
  hasStation: Boolean!
}
